{
  "name": "empyrean-hero",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "pnpm --parallel run dev",
    "build": "pnpm --filter @empyrean-hero/engine build && pnpm --filter @empyrean-hero/client build && pnpm --filter @empyrean-hero/server build",
    "test": "pnpm -r run test",
    "typecheck": "pnpm -r run typecheck"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
